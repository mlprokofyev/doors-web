---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Doors.">
  <main class="landing">
    <h1 class="landing__title">Doors.</h1>
    <a href="/doors" class="landing__enter">Enter</a>
  </main>
  <footer class="landing-footer">
    <a href="https://t.me/justhappymisha" class="landing-footer__link" target="_blank" rel="noopener">by justhappymike</a>
  </footer>
</BaseLayout>

<style>
  .landing {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100dvh;
    gap: 3rem;
  }

  .landing__title {
    font-size: clamp(2rem, 8vw, 5rem);
    color: var(--color-text);
    letter-spacing: 0.1em;
  }

  .landing__enter {
    font-family: var(--font-pixel);
    font-size: clamp(0.6rem, 2vw, 0.9rem);
    color: var(--color-text-muted);
    border: 1px solid var(--color-text-muted);
    padding: 0.8em 2em;
    transition: color 0.2s, border-color 0.2s;
  }

  .landing__enter:hover,
  .landing__enter:focus-visible {
    color: var(--color-accent);
    border-color: var(--color-accent);
    outline: none;
  }

  .landing-footer {
    position: fixed;
    bottom: 1.2rem;
    left: 0;
    right: 0;
    text-align: center;
  }

  .landing-footer__link {
    font-family: var(--font-pixel);
    font-size: clamp(0.4rem, 1vw, 0.55rem);
    color: var(--color-text-muted);
    transition: color 0.2s;
  }

  .landing-footer__link:hover {
    color: var(--color-text);
  }
</style>
